defmodule Exp.Action.Formatter do
    require Logger
    
    @moduledoc """
        Formatting of output and visuals in terminal.
    """

    def format_output() do
        
    end


    def format_entries(entries) do
        entries
    end


    
end